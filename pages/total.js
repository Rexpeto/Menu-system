import Layout from "@/layout/Layout";

const Total = () => {
    return (
        <Layout title="Total">
            <h1 className="text-4xl font-bold">Total</h1>
        </Layout>
    );
};

export default Total;
